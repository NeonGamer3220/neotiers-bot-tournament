---
name: sync-db
description: Initialize/sync database schema
category: deployment
---

```bash
# Sync database schema
# This script initializes the database tables using schema.sql
# Note: You need to manually run the schema.sql in Supabase SQL editor
# or use supabase CLI to apply migrations.

echo "Database sync:"
echo "Please run the schema.sql file in your Supabase SQL editor:"
echo "  - Go to your Supabase project dashboard"
echo "  - Navigate to SQL Editor"
echo "  - Execute the contents of schema.sql"
echo ""
echo "Or use the Supabase CLI:"
echo "  supabase db push"
```
